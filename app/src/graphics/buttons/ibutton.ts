/// <reference path="../../../lib/phaser.comments.d.ts"/>

interface IButton {
    /**
     * Move the button to the given horizontal position
     * 
     * @param {number} x where the left side of the button goes.
     * @memberof IButton
     */
    moveTo(x:number) : void;
}
